<%- include('layouts/header') -%>

<div class="hero min-h-[60vh] bg-base-100">
  <div class="hero-content text-center">
    <div class="max-w-md">
      <h1 class="text-5xl font-bold text-error">Oops!</h1>
      <p class="py-6 text-lg">Terjadi kesalahan pada server.</p>
      <% if (message) { %>
      <p class="text-red-500 mb-4"><%= message %></p>
      <% } %> <% if (stack) { %>
      <pre
        class="bg-gray-800 text-white p-4 rounded-lg text-left overflow-auto mt-4"
      >
<%= stack %></pre
      >
      <% } %>
      <a href="/" class="btn btn-primary mt-6">Kembali ke Beranda</a>
    </div>
  </div>
</div>

<%- include('layouts/footer') -%>